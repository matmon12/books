# Books Management - Тестовое задание Frontend разработчик

Проект демонстрирует реализацию двух универсальных composables на Vue 3 и TypeScript в рамках тестового задания.

## 📋 Выполненные задания

### 1. ✅ Универсальный composable для валидации форм (`useFormValidation`)

**Расположение:** `src/composables/useFormValidation.ts`

**Функциональность:**

- Проверка данных формы по заданным правилам (список полей и правила для каждого поля)
- Поддержка любого количества полей и правил валидации
- Реактивное состояние валидности формы и каждого поля
- Состояние `touched` для отслеживания взаимодействия пользователя с полями
- Автоматическая валидация при изменении данных (если есть ошибки)

**Основные возможности:**

- Валидация на уровне поля и формы
- Кастомные правила валидации с сообщениями об ошибках
- Реактивное состояние (`isValid`, `hasErrors`, `isTouched`)
- Методы: `validateField`, `validateForm`, `resetField`, `resetForm`, `touchField`

**Пример использования:**

```typescript
const formData = reactive({ title: '', author: '', year: null })

const validationRules: ValidationConfig<typeof formData> = {
  title: [
    {
      validator: (v) => !!(v && v.trim().length),
      message: 'Название обязательно',
    },
    {
      validator: (v) => !v || v.trim().length >= 2,
      message: 'Минимум 2 символа',
    },
  ],
  author: [
    { validator: (v) => !!(v && v.trim().length), message: 'Автор обязателен' },
  ],
  year: [
    { validator: (v) => v != null, message: 'Год обязателен' },
    {
      validator: (v) => v == null || v > 1000,
      message: 'Год должен быть > 1000',
    },
  ],
}

const { fieldStates, validateForm, isValid, isTouched } = useFormValidation(
  formData,
  validationRules,
)
```

### 2. ✅ Универсальный composable для обращения к API (`useApi`)

**Расположение:** `src/composables/useApi.ts`

**Функциональность:**

- Принимает необходимые параметры для HTTP запроса (адрес, метод, заголовки, тело запроса, параметры)
- Возвращает реактивные данные запроса, статус, состояния загрузки, успеха, ошибки
- Поддержка всех HTTP методов: GET, POST, PUT, PATCH, DELETE
- Базовая конфигурация для переиспользования

**Основные возможности:**

- Реактивные состояния: `data`, `error`, `status`, `statusCode`
- Computed свойства: `isLoading`, `isSuccess`, `isError`, `isIdle`
- Удобные методы: `get`, `post`, `put`, `patch`, `del`
- Автоматическое объединение заголовков из базовой конфигурации и запроса
- Построение URL с query параметрами
- Обработка HTTP ошибок и сетевых ошибок

**Пример использования:**

```typescript
// С базовой конфигурацией
const api = useApi<Book[]>({
  url: 'https://api.example.com/books',
  headers: { Authorization: 'Bearer token' },
})

// GET запрос
await api.get()

// POST запрос
await api.post(undefined, { title: 'Новая книга', author: 'Автор' })

// PUT запрос с полным URL
await api.put('https://api.example.com/books/1', { title: 'Обновленная книга' })

// DELETE запрос
await api.del('/1')
```

## 🎯 Демо-проект

Реализовано приложение для управления книгами, демонстрирующее использование обоих composables:

- **Форма добавления книги** (`src/components/Books/BooksForm.vue`)
  - Использует `useFormValidation` для валидации полей
  - Использует `useApi` для отправки данных на сервер
  - Поля: название, автор, год издания, количество страниц, описание

- **Таблица книг** (`src/components/Books/BooksTable.vue`)
  - Использует `useApi` для загрузки списка книг
  - Функционал удаления

- **Главная страница** (`src/views/HomePage.vue`)
  - Оркестрация компонентов формы и таблицы
  - Обновление таблицы после успешного добавления книги

## 🚀 Быстрый старт

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev
```

## 🛠️ Технологии

- **Vue 3.5** с Composition API
- **TypeScript 5.8** со строгим режимом
- **Vite 7** для сборки
- **SCSS** для стилей

## 📁 Структура проекта

```text
src/
├── composables/
│   ├── useFormValidation.ts  # Composable для валидации форм
│   └── useApi.ts             # Composable для HTTP запросов
├── components/
│   ├── Books/
│   │   ├── BooksForm.vue     # Форма для книг
│   │   └── BooksTable.vue    # Таблица книг
│   └── UI/                    # Переиспользуемые UI компоненты
├── types/
│   └── books.ts              # Типы для книг
└── views/
    └── HomePage.vue          # Главная страница
```

